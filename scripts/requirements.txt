# article-generator skill 依赖

# YAML 解析（review_selfcheck 解析 frontmatter）
PyYAML>=6.0

# 图片生成（nanobanana）
google-genai>=0.1.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# 进度显示
tqdm>=4.65.0

# 重试机制
tenacity>=8.2.0

# 网页截图（shot-scraper + Playwright）
# shot-scraper: 轻量 CLI 工具
# playwright: 完整浏览器渲染（智能截图用，安装后需运行: playwright install chromium）
shot-scraper>=1.0
playwright>=1.40.0

# GitHub Token 验证（可选）
requests>=2.31.0

# S3 上传（可选）
boto3>=1.26.0  # optional: S3 upload support
